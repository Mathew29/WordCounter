@using WordCounter.Models;

<h1>Word Counter</h1>
<ul>
@if (Model.Count == 0)
{
  <p>You have no Words to Count!</p>
}
@if(Model.Count != 0)
{
  @foreach (var item in Model)
  {
    <li><a href='/wordcounters/@item.GetId()'>@item.GetName()</a></li>
  }
}
</ul>
<a href="/wordcounters/new">Add a new Word and Sentence.</a>

<form action="/wordcounters/delete" method="post">
  <button type="submit" name="button">Clear All Items</button>
</form>me</a></p>
